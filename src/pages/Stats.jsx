import { Box, Typography } from '@mui/material';

export default function Stats() {
  return (
    <Box sx={{ maxWidth: 600, mx: 'auto', mt: 4, mb: 4, p: 2 }}>
      <Typography variant="h4" gutterBottom>統計ページ（仮実装）</Typography>
      <Typography variant="body1">今後ここに統計・ダッシュボード機能を実装予定です。</Typography>
    </Box>
  );
} 